---
name: 圖片辨識系統操作手冊
description: 本機圖片辨識系統（上傳圖片／相機拍照／語音查詢找最相似圖片）的啟動、新增圖片、除錯方式
metadata:
  type: runbook
---

# 圖片辨識系統操作手冊

適用資料夾：`C:\20260923\圖片辨識資料夾`

## 一、啟動系統

1. 雙擊 `start.bat`（或在 PowerShell 執行 `server.ps1`）
2. 系統會自動開啟瀏覽器到 `http://localhost:8787`（建議使用 **Chrome 或 Edge**，語音查詢功能需要這兩者其一）
3. 若瀏覽器沒有自動開啟，手動輸入網址即可
4. 第一次開啟會看到右上角顯示「建立索引中... (x/230)」，這是在為每張圖片計算特徵向量，等它變成綠色「索引就緒」就可以開始查詢
5. 關閉方式：回到跳出的黑色命令視窗，按 `Ctrl+C`，或直接關閉該視窗

## 二、三種查詢方式怎麼用

| 分頁 | 操作 |
|------|------|
| 📁 上傳圖片 | 點選檔案 → 自動比對並顯示前 5 名最相似圖片 |
| 📷 相機拍照 | 按「啟動相機」→ 允許瀏覽器使用相機 → 按「拍照並查詢」 |
| 🎤 語音查詢 | 按「開始語音查詢」→ 允許瀏覽器使用麥克風 → 說出料號（例如「兩百八六六」對應 200866）。若麥克風無法使用，直接在下方文字框輸入料號按「查詢」也可以 |

## 三、新增圖片到資料庫

1. 把新的圖片檔（`.jpg` / `.jpeg` / `.png` / `.webp`）直接放進 `圖片辨識資料夾` 這個資料夾（跟 `index.html` 同一層，不要放進 `vendor` 子資料夾）
2. 重新整理網頁，系統會自動偵測到新圖片並幫它算特徵向量（或按右上角「重建索引」按鈕強制重新掃描）
3. 圖片檔名請直接用料號命名（例如新料號 `200999` 存成 `200999.jpg`）。語音/文字查詢預設會用檔名（即料號）當關鍵字比對，不需要另外維護 `tags.json`；如果想額外補充中文說明關鍵字，可仿照格式加入，例如：
   ```json
   "200999.jpg": ["新物品說明", "關鍵字1"]
   ```
4. 上傳圖片／相機拍照的視覺比對**不需要**改 `tags.json`，新圖片放進資料夾就會自動被索引。

## 四、常見問題排除

**症狀：畫面一直卡在「載入辨識模型中...」或「建立索引中...」不動**
→ 開瀏覽器開發者工具（F12）看 Console 有沒有紅字錯誤。常見原因：`vendor/mobilenet_v1_1.0_224/` 底下的檔案被誤刪或不完整（應該有 `model.json` + 54 個 `groupN-shard1of1` 檔案，合計約 17MB）。

**症狀：相機/麥克風按下去沒反應，或瀏覽器說權限被拒**
→ 確認網址列是 `http://localhost:8787`，不是直接用「開啟檔案」打開 `index.html`（`file://` 開頭的網址瀏覽器會擋掉相機/麥克風權限，這是瀏覽器安全機制，不是系統的錯）。也要確認瀏覽器網址列左側的相機/麥克風權限沒有被手動封鎖過。

**症狀：語音辨識按鈕整個是灰色、按不下去**
→ 目前瀏覽器不支援語音辨識（例如 Firefox）。請改用 Chrome 或 Edge，或直接用下方的文字輸入框查詢（效果相同）。

**症狀：雙擊 `start.bat` 後視窗一閃就關掉，或出現 `Unexpected token '}'` 之類的錯誤**
→ 通常是 `server.ps1` 被存成不含 BOM 的 UTF-8 編碼，導致 Windows PowerShell 5.1 誤判編碼、把中文字串解析壞掉。修法：用支援「UTF-8 with BOM」的編輯器（例如記事本另存新檔時選「UTF-8 (有 BOM)」）重新儲存 `server.ps1`。

**症狀：查詢結果看起來都不太準**
→ 目前的視覺比對用的是通用的 MobileNet 特徵（不是針對這批物料訓練的專用模型），對外觀差異明顯的料號通常很準，但外觀相近的不同料號（例如同系列不同規格）有時會分不清楚，這是預期中的限制，不是系統故障；建議查看前 5 名候選並由人工確認最終料號。

**症狀：想要清空重算所有索引**
→ 按網頁右上角「重建索引」按鈕，或在瀏覽器開發者工具 Console 輸入 `localStorage.removeItem('img_recognition_index_v1')` 後重新整理頁面。

## 五、系統邊界（不是故障，是設計上的取捨）

- 語音辨識會透過瀏覽器內建功能連到雲端做語音轉文字（不是 100% 離線），但圖片比對本身完全在本機運算，不會把任何圖片上傳到任何地方。
- 這是給單一使用者在自己電腦上用的工具，沒有帳號/多人協作機制。

## 六、線上版本（GitHub Pages）

- 網址：https://ysc0426000-blip.github.io/py_image/ （手機/平板/任何裝置只要有瀏覽器都能開，不需要先啟動本機伺服器）
- GitHub repo：https://github.com/ysc0426000-blip/py_image （public）

**要更新線上版本時（改了程式碼或新增/刪除了圖片之後）：**

1. 在 PowerShell 切到專案資料夾，先執行：
   ```powershell
   .\generate_images_json.ps1
   ```
   這會重新產生 `images.json`（線上版本讀圖片清單用的靜態檔案，跟本機的 `/api/images` 是分開的兩條路，忘記跑這步的話，線上版本不會看到新加的圖片）。
2. 接著執行（PATH 裡如果還找不到 `git`，先執行第一行把 Git 加進當次 PowerShell 的 PATH）：
   ```powershell
   $env:Path = "C:\Program Files\Git\cmd;C:\Program Files\Git\bin;C:\Program Files\GitHub CLI;" + $env:Path
   git add -A
   git commit -m "描述這次改了什麼"
   git push
   ```
3. push 之後 GitHub Pages 會自動重新建置，大約 30 秒到 1 分鐘後 https://ysc0426000-blip.github.io/py_image/ 就會是最新版本（可以到 repo 的 Actions 分頁或 `Settings > Pages` 看建置狀態）。

**注意**：這個 repo 是 public 的（GitHub 免費方案的 Pages 只能從 public repo 發佈），所以任何人都能看到原始碼和 `vendor/` 裡的模型檔、以及所有圖片。如果之後有不想公開的圖片，不要放進這個資料夾。

更完整的技術架構說明請見同資料夾的 `CLAUDE.md`。
