# 本機圖片辨識系統

## 系統目標

在本機（不依賴雲端 API）執行的圖片辨識系統。使用者可以透過三種方式查詢工作目錄中「最相似」或「最相關」的圖片：

1. **上傳圖片** — 選擇一張圖片，比對視覺相似度
2. **相機拍照** — 用裝置相機即拍即查
3. **語音查詢** — 說出關鍵字（例如「螺絲」「狗」「鋸片」「汽車」），比對圖片標籤

## 環境限制（重要）

開發與執行當下，本機 **沒有安裝 Python 或 Node.js**（只有 Windows Store 的執行別名 stub）。
因此本系統刻意設計為 **純瀏覽器端（client-side）應用**：

- 沒有 Flask / FastAPI，也沒有任何後端 AI 框架
- 唯一的「伺服器」是 `server.ps1`，一個用 PowerShell `System.Net.HttpListener` 寫的極簡靜態檔案伺服器
- 所有 AI 運算（MobileNet 圖片特徵擷取）都在瀏覽器內用 TensorFlow.js 執行

若未來這台機器安裝了 Python/Node，**不需要**為了「看起來更正式」而重寫成後端服務——目前的架構單純且已驗證可行，除非有新需求（例如需要伺服器端批次處理、多人共用同一份索引等）才需要調整。

## 架構

```
圖片辨識資料夾/
├── index.html          # 主頁面（三個分頁：上傳/相機/語音）
├── app.js              # 前端邏輯：模型載入、索引建立、三種查詢方式
├── style.css            # 樣式
├── tags.json             # 每張圖片的中文關鍵字（供語音/文字查詢比對）
├── server.ps1           # 本機靜態網頁伺服器（HttpListener），含 /api/images 動態端點
├── start.bat            # 雙擊即可啟動伺服器並開啟瀏覽器
├── vendor/
│   ├── tf.min.js                      # TensorFlow.js（本機端，離線）
│   └── mobilenet_v1_1.0_224/          # 本機端 MobileNet v1 權重（55 個 shard + model.json，約 17MB）
├── A001_1.jpg ... E003_1.jpg          # 圖片資料庫（現有 15 張，5 類 × 3 項）
├── CLAUDE.md            # 本檔案
└── SKILL.md             # 操作手冊
```

### 圖片資料庫內容

圖片檔名已依類別編碼（字母=類別，數字=同類別中的第幾項）：

| 類別 | 內容 |
|------|------|
| A001–A003 | 螺絲/螺栓（六角螺栓、滾花螺絲、十字自攻螺絲） |
| B001–B003 | 狗（杜賓犬、瑪連萊犬、白色貴賓幼犬） |
| C001–C003 | OAV 木工機械（精密裁板鋸、封邊機、立式帶鋸機） |
| D001–D003 | 鋸片（粗齒圓鋸片、精密細齒鋸片、迷你鋸片） |
| E001–E003 | 汽車（Honda HR-V、Tesla Model S、Toyota RAV4） |

新增圖片時只要把 `.jpg/.jpeg/.png/.webp` 檔放進本資料夾根目錄即可，`server.ps1` 的 `/api/images` 端點會自動掃描到。若要讓語音/文字查詢也認得新圖片，需在 `tags.json` 補上對應的中文關鍵字（否則系統會退回用檔名本身當關鍵字）。

## 視覺相似度查詢（上傳圖片 / 相機拍照）

- 模型：MobileNet v1 (alpha=1.0, 224×224)，取 `global_average_pooling2d_1` 層輸出作為 1024 維特徵向量（不使用最後的 1000 類分類層）
- 前處理：置中裁切成正方形 → 縮放到 224×224 → 依 Keras MobileNet 慣例正規化到 [-1, 1]
- 相似度：特徵向量 L2 正規化後取內積（等同 cosine similarity）
- 索引：頁面載入時，會對資料夾內每張圖片各算一次特徵向量，快取到瀏覽器 `localStorage`（key: `img_recognition_index_v1`）。之後只有新增/修改過的圖片（依檔案大小與修改時間判斷）才會重算，不需每次都重建索引。畫面右上角「重建索引」按鈕可強制全部重算。

## 語音 / 文字查詢

- 語音辨識使用瀏覽器內建的 `webkitSpeechRecognition`（Web Speech API），語言設定為 `zh-TW`。
- **已知限制**：Web Speech API 在 Chrome/Edge 上會把錄到的語音送到瀏覽器廠商的雲端做辨識，**不是完全離線**。本機沒有 Python/Whisper 可用，因此這是在目前環境限制下最務實的作法。若之後需要完全離線語音辨識，可考慮改用 Vosk 或本機 Whisper（需要 Python 或额外的 WASM 方案），屆時需重新設計這部分。
- 語音辨識出的文字，或使用者直接輸入的文字，會拿去跟 `tags.json` 的中文關鍵字比對（子字串比對，比對到的關鍵字越長分數越高），回傳最相關的前 5 筆結果。這條路徑**不需要**視覺模型，因此即使 MobileNet 模型載入失敗，語音/文字查詢仍可運作。
- 不支援語音辨識的瀏覽器（例如 Firefox）會自動停用麥克風按鈕，並提示改用下方文字輸入框——文字輸入框在任何瀏覽器都可用，也適合沒有麥克風的情境。

## 為什麼要有 server.ps1（不能直接雙擊開 index.html 嗎？）

不行。瀏覽器的相機（`getUserMedia`）與麥克風權限要求「安全來源」（https 或 localhost），用 `file://` 直接開啟 HTML 檔案通常會被瀏覽器擋下相機/麥克風權限。`server.ps1` 用 `http://localhost:8787` 提供服務，滿足這個要求，同時也讓 `fetch()` 讀取 `tags.json`、`/api/images`、模型檔案時不會遇到 `file://` 的 CORS 限制。

`server.ps1` 存成 UTF-8 with BOM 編碼——若未來要修改這支腳本，請務必保留 BOM，否則 Windows PowerShell 5.1 會因為看到 UTF-8 但當作系統內碼（Big5/ANSI）解析，把腳本內的中文字串解析壞掉，導致 `Unexpected token '}'` 之類的語法錯誤（開發時已實際踩過這個坑）。

## 已知限制 / 未來可改進方向

- 相似度比對是「一張查詢圖 vs 資料庫全部圖片」線性掃描，圖片數量少（15 張）沒有效能問題；若圖片數量成長到數千張以上，需要改用近似最近鄰（ANN）索引。
- 目前每個瀏覽器分頁各自快取索引在自己的 `localStorage`，不同瀏覽器/裝置間不會共用索引（各自建立一次即可，之後靠快取加速）。
- 語音查詢仰賴 `tags.json` 手動維護的關鍵字，不是真正的「語音轉圖片」語意理解；如果之後想要「用一句話描述外觀就能搜圖」的效果，需要換成 CLIP 這類圖文對齊模型（此環境目前無法穩定下載/驗證這類較大的模型，因此未採用）。
